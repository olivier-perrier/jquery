{{> admin/header}}

<div class="container-fluid">

    <div class="row">

        <div class="col-2 pl-0">
            {{> admin/nav}}
        </div>

        <div class="col-10 ">
            {{> @partial-block }}
        </div>
    </div>
</div>

{{> footer}}