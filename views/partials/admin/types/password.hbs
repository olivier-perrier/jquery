<div class="form-group row">
    {{> admin/types/field-label}}
    <div class="col-sm-10">
        <input type="password" class="form-control" id="post-{{@key}}" value="{{value}}" {{disabled}}>
        <small class="form-text text-muted">{{comment}}</small>
    </div>
</div>